# Core dependencies
extract-msg
openpyxl
PyYAML==6.0.3
pywin32
xlrd

# Git integration
pygit2

# PDF processing (study/)
pandas
pdfplumber
PyMuPDF

# Image preprocessing + OCR (study/)
opencv-python
pytesseract
# Tesseract binary required separately:
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#   conda   : conda install -c conda-forge tesseract

# If switching to a DL-based extractor (easyocr, surya-ocr, python-doctr),
# install CPU torch first so the .venv stays independent of the base XPU env:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
